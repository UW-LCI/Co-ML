# DataModels

This package defines various data models to be used in the app.
This package should be free of both business logic and dependencies (i.e., it is intended to be a leaf package only declaring types and nothing else)
